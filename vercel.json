{ 
  "$schema": "https://openapi.vercel.sh/vercel.json", 
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "buildCommand": "yum install -y traceroute",
  "functions": {
    "api": {
      "runtime": "@vercel/node"
    }
  },
  "outputDirectory": "api"
} 